specVersion: "v1beta"
serviceId: "angular"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
